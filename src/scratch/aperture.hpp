// aperture generator (uses OpenGL)

#ifndef APERTURE_H
#define APERTURE_H

#include "image.hpp"

class aperture
{
public:
    //aperture();
    //
    static image generate(size_t width, size_t height, int sides);

private:
    //
};

#endif
